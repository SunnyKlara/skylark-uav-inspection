# 实验设计 v2 — 严谨版

> 给"理论先行、不注水、不降低标准"的论文重设计。
> 写完先给用户审，**审过才动代码**。
> 撰写日期：2026-05-27

---

## 0. 写在最前 — 三条底线

1. **理论必须经验证**。所有"小目标"、"长尾"、"算力受限"等论断必须由数据集实测支撑，不能照抄文献模板。
2. **实验必须公平**。所有 baseline / ablation / ours 严格同协议（同 epoch / 同 LR / 同 augment / 同优化器）。
3. **数据不漂亮也照写**。如果实验显示某项改进无效，论文里就**写它无效**，并分析为什么——这本身就是一个学术贡献。

---

## 1. 论文重新定位

### 1.1 旧定位（撤销）

> "本文方法相较 baseline 取得 X pp 提升"——把全文绑在了"性能 SOTA"承诺上。撤掉。

### 1.2 新定位

**论文标题不变**，但**核心问题陈述变更**为：

> 本文不追求绝对 SOTA，而是面向 **PVEL-AD 数据集 + nano 算力预算**，
> 系统验证三组工程问题：
>
> Q1：在 PVEL-AD 数据集的真实尺度分布下，**P2 高分辨率分支**对 finger 类（占训练框 37%、中位尺寸 28.9 px、最小 14.5 px）目标的检测贡献多大？
>
> Q2：**层次化 CBAM 注入（P3+P4+P5）** 与单层级注入相比，在 nano 算力预算下贡献分布如何？相比同体量的 EMA 注意力又如何？
>
> Q3：**预训练权重迁移率随结构改动的衰减**，对训练预算有何量化要求？工程实践中应如何应对？

这三个问题**和性能数字无关**——它们关心的是"机制是否有效 / 经验是否可推广"。这才是本科毕设/期刊论文真正应该回答的问题。

### 1.3 撤回的具体表述

| 原文位置 | 旧措辞 | 新措辞 |
|---|---|---|
| 摘要 | "提升 X pp / SOTA" | "在受限算力下系统验证三组工程问题，给出量化结论" |
| 1.3.2 创新点 | "性能提升的方法" | "针对 PV 检测场景三个工程问题的系统消融研究" |
| 4 章实验 | "本文方法 SOTA" | "在公平协议下对照 baseline，揭示改进点的真实贡献" |
| 5 章结论 | "提升 X pp" | "本文不寻求 SOTA，而是给出三组可推广的工程经验" |

---

## 2. 数据集真实事实（核对版）

> 旧论文里写的"4-8 像素小目标"、"90% box < 0.05"、"7000 倍长尾"全是**错的或夸大的**。
> 以下是基于 train (12682 imgs, 6254 boxes) + val (3171 imgs, 1588 boxes) 的实测数据。

### 2.1 标注框尺寸分布（训练集 6254 框，imgsz=640）

| 分位 | 归一化 side | 640 像素 | 1024 像素 |
|---|---|---|---|
| min | 0.0227 | 14.5 | 23.3 |
| p10 | 0.0381 | 24.4 | 39.0 |
| p25 | 0.0462 | 29.6 | 47.3 |
| **p50（中位）** | **0.0634** | **40.6** | **64.9** |
| p75 | 0.1594 | 102.0 | 163.2 |
| p90 | 0.8901 | 569.7 | 911.5 |
| max | 0.9932 | 635.6 | 1017.0 |

**关键观察**：
- ≤ 8 px 占比 = **0%**（不是论文以前说的"主导"）
- ≤ 16 px 占比 = **0.2%**（P3 stride=8 完全覆盖）
- ≤ 32 px 占比 = **32.4%**（属于 COCO small object 区间）
- p90 > 0.5 — **数据集是双峰分布**：要么很小（finger 类），要么很大（black_core / short_circuit / dislocation）

### 2.2 类别分布（训练集）

| 类别 | 框数 | 中位边长 (640px) | 最小边长 (640px) | 适合检测层 |
|---|---|---|---|---|
| finger | **2317 (37%)** | **28.9** | **14.5** | **P3 边界（关键）** |
| crack | 1010 | 50.0 | 14.6 | P3-P4 |
| black_core | 826 | 557.0 | 28.5 | P5 |
| thick_line | 776 | 37.9 | 20.0 | P3 |
| horizontal_dislocation | 678 | 94.4 | 72.6 | P4 |
| short_circuit | 404 | 604.1 | 588.7 | P5 |
| star_crack | 110 | 48.5 | 28.1 | P3-P4 |
| vertical_dislocation | 101 | 95.9 | 48.6 | P4 |
| printing_error | 16 | 29.5 | 25.7 | P3 |
| corner | 7 | 86.7 | 37.2 | P3-P4 |
| fragment | 7 | 243.7 | 31.6 | P4-P5 |
| scratch | 2 | 247.9 | 103.3 | P4 |

**长尾比例**：max(2317) / min(2) = **1158.5×**（不是 7000×；7000× 是 PVEL-AD 全集层面，本文训练集不是）

### 2.3 P2 分支的真实动机（修正后）

旧动机（错）：4-8 px 小目标常规检测器看不见。

**新动机（实测支撑）**：

> finger 是 PVEL-AD 训练集中**最多发**的缺陷类（37% 框、2317 个），其**中位边长 28.9 px、最小 14.5 px、p10 仅 18 px**。在 imgsz=640 下，这些目标在 P3（stride=8）输出特征图上仅占 2-4 个网格单元，判别力受限。**P2（stride=4）将这些目标的特征单元数提升至 4-8 个，理论上能改善小尺寸 finger 类的召回率。**
>
> 同时数据集呈现明显双峰：47% 的框 side > 0.1（即 64+ px），P5 大目标分支同样必要。**因此本文保留 P3/P4/P5 三层并新增 P2，构成四尺度结构。**

这个动机**只针对 finger 类**——不能再说"全数据集小目标"。论文写法也要改：消融分析必须**单独报告 finger 类的 AP 提升**，而不是仅看整体 mAP。

---

## 3. 实验组设计

### 3.0 公共设置

**硬件 / 框架**：RTX 5060 Ti 16GB / PyTorch 2.7.1 cu128 / ultralytics 8.4.54。
**数据**：PVEL-AD train (12682 imgs) / val (3171 imgs) / test (19150 imgs，仅最终评估用一次)。
**imgsz**：640。
**batch / workers**：8 / 2（5060 Ti + Windows 稳定配置）。

**统一训练协议（v2）**：
| 超参 | 值 | 改动原因 |
|---|---|---|
| epochs | 200 | 旧 50/80 不够 from-scratch 子网络收敛 |
| optimizer | SGD | 默认 |
| lr0 | 0.01 | 默认 |
| **lrf** | **0.01** | 默认（即 final lr = 1e-4，避免完全归零） |
| **cos_lr** | **True** | 旧 linear LR 在 80 ep 退到 6.7e-4 后期相当于 0 |
| momentum | 0.937 | 默认 |
| weight_decay | 5e-4 | 默认 |
| warmup_epochs | 3 | 默认 |
| **close_mosaic** | **20** | 提前关 mosaic 让模型在干净数据上收敛 |
| **patience** | **30** | 30 epoch 无改善早停（节省时间） |
| pretrained | yolo11n.pt | **所有配置**都用同一预训练源（v8/v10/v11 各自用对应 pt） |
| AMP | True | 默认 |
| seed | 42 | 复现 |

**输出位置**：`runs/v2/`（旧 `runs/` 完整保留作为"短预算/线性 LR"对照组）。

---

### 3.1 实验组 E1 — Baseline 横评

**问题**：在公平协议下，YOLOv8n / YOLOv10n / YOLOv11n 中谁该当 baseline？

**配置**：3 个 baseline × 200ep × v2 协议（各自用 yolov8n.pt / yolov10n.pt / yolo11n.pt）

**预期产出**：
- 表 4.2 完整 mAP@0.5 / mAP@0.5:0.95 / Precision / Recall / FPS / 参数量
- 选择 mAP@0.5:0.95 最高者作为后续 baseline（保持选 v11n 的逻辑）

**可能结果**：
- 任一 baseline ≥ 现有 50ep 结果（多训 4× 应当不差）
- 选定的 baseline 数字作为后续所有对照的"参考线"

**时间预算**：3 × 15h = **45 小时**

---

### 3.2 实验组 E2 — 主消融（A0-A4）

**问题**：CBAM 单独 / EMA 单独 / P2 单独 / CBAM+P2 各自的贡献？

| 配置 | CBAM | EMA | P2 | 描述 |
|---|---|---|---|---|
| A0 | × | × | × | yolo11n baseline 重训 |
| A1 | ✓(P3+P4+P5) | × | × | + 层次化 CBAM |
| A2 | × | ✓(P3+P4+P5) | × | + 层次化 EMA（CBAM 对照） |
| A3 | × | × | ✓ | + P2 分支 |
| A4 | ✓(P3+P4+P5) | × | ✓ | 全集成 (= ours) |

**关键约束**：
- 5 个配置都用 yolo11n.pt 预训练
- 都用 v2 协议（200ep / cos_lr）
- A0 用 ultralytics 标准 yolo11n.yaml；A1-A4 用 configs/yolo11n_*.yaml（已存在）
- 报告**整体 mAP** + **finger 类 AP**（验证 P2 的针对性动机）

**预期产出**：
- 表 4.3 五行消融结果
- finger 类 AP 单列（验证创新点 2 真实动机）
- A1 vs A2 比较给出 CBAM vs EMA 的结论
- A4 vs A1+A3 比较给出"协同/独立/冲突"的结论

**可能结果（必须诚实报告）**：

| 情境 | 结论 | 论文叙事 |
|---|---|---|
| A1 > A0, A3 > A0, A4 > A1+A3 | 改进有效，超线性叠加 | 方法成功 |
| A1 > A0, A3 > A0, A4 ≈ A1+A3 | 改进有效，独立叠加 | 方法成功（次优） |
| A1 ≈ A0, A3 > A0, A4 > A0 | CBAM 无效，P2 有效 | 撤回 CBAM 创新点 |
| A1 > A0, A3 ≈ A0, A4 ≈ A1 | CBAM 有效，P2 无效 | 撤回 P2 创新点 |
| 全部 ≈ A0 | 改进无效 | 论文转为"消融发现：在 nano 规模下结构改进收益有限" |

**底线**：任何上述情境都按事实写。不允许调超参直到 A4 > A0。

**时间预算**：5 × 15h = **75 小时**

---

### 3.3 实验组 E3 — CBAM 注入位置消融（验证创新点 1）

**问题**：层次化（P3+P4+P5）是否真的比单层级好？

**配置**（基于 yolo11n + 不同 CBAM 位置）：
| 配置 | CBAM 位置 |
|---|---|
| A1a | 仅 P3 |
| A1b | 仅 P5 |
| A1c | P3+P4 |
| A1（已在 E2） | P3+P4+P5 |

**关键**：这是创新点 1 的"必要性证据"。如果 A1a（仅 P3）≈ A1（三层），那么"层次化"叙事不成立，论文要改成"单层级 CBAM 已足"。

**实施**：需要新增 3 个 yaml 配置（修改现有 yolo11n_cbam.yaml）。

**时间预算**：3 × 15h = **45 小时**

---

### 3.4 实验组 E4 — 训练预算扫描（揭示工程经验）

**问题**：ours（A4）需要多长训练才能收敛？

**配置**：仅 ours，不同 epoch 数：
- 50 epoch（已有：旧 daemon 跑的 ours? **不**——旧的是 80ep linear，不能复用）
- 100 epoch
- 200 epoch（在 E2 中）
- 300 epoch
- 400 epoch（如果 300 还在涨）

**关键产出**：
- 训练曲线图，给出"边际收益拐点"
- 对应 baseline（标准结构、100% 迁移率）的曲线图
- 给后人具体经验：改了 ultralytics yaml 后预期需要多长 epoch

**时间预算**：
- 100ep ≈ 7.5h
- 300ep ≈ 22h
- 400ep（如需）≈ 30h
- 总计 **30-60 小时**

---

### 3.5 实验组 E5 — From-scratch 对照（验证创新点 3 的工程经验）

**问题**：预训练权重对结构改动型方法有多重要？

**配置**：ours（A4）从随机初始化训 300ep，对照 ours from yolo11n.pt 200ep。

**关键产出**：
- 量化"yolo11n.pt 给 ours 节省了多少 epoch"
- 给出"自定义 yaml 改动后预训练迁移率与 epoch 预算关系"的工程结论

**时间预算**：1 × 22h = **22 小时**

---

### 3.6 评估组（不训练）

#### E6 — 复杂度
- params / GFLOPs / FPS（5060 Ti, batch=1, imgsz=640）
- 4 配置：v8n / v10n / v11n / ours
- 工具：`eval/eval_complexity.py`（已写）
- 时间：< 30 分钟

#### E7 — 鲁棒性
- 6 扰动 × 3 强度 × 2 模型（v11n baseline / ours）
- 工具：`eval/eval_robustness.py`（已写）
- 时间：< 1 小时

#### E8 — 部署
- FP32 / FP16 / ONNX × ours
- 工具：`eval/eval_deployment.py`（已写）
- 时间：< 30 分钟

#### E9 — 可视化
- 训练曲线 / Grad-CAM / 定性对比
- 工具：`visualize/{plot_results,grad_cam,make_qualitative}.py`（已写）
- 时间：< 30 分钟

---

## 4. 时间预算总表

| 组 | 内容 | 配置数 | epoch | 时间 (h) |
|---|---|---|---|---|
| E1 | baseline 横评 | 3 | 200 | 45 |
| E2 | 主消融 A0-A4 | 5 | 200 | 75 |
| E3 | CBAM 位置消融 | 3 | 200 | 45 |
| E4 | 预算扫描 (ours) | 3 | 100/300/400 | 60 |
| E5 | from-scratch 对照 | 1 | 300 | 22 |
| E6-E9 | 评估+可视化 | - | - | 3 |
| **小计训练** | | 15 | | **247 h ≈ 10.3 天** |

**关键**：E1 和 E2 是必做核心，**E3/E4/E5 是验证"理论严谨性"的非必做但强烈推荐**——没有 E3，创新点 1（层次化）站不住；没有 E4，训练预算解释不清；没有 E5，工程经验贡献站不住。

**优先级**：
- 必做：E1 + E2 + E6-E9 = **123 小时 / 5.1 天**
- 推荐做：必做 + E3 + E4 = **228 小时 / 9.5 天**
- 完整：上面全部 = **250 小时 / 10.4 天**

---

## 5. 论文章节调整方案

### 5.1 摘要重写

> 光伏发电...（背景不动）...本文以 YOLOv11n 为基线，**针对 PVEL-AD 数据集真实尺度分布与 nano 算力预算约束**，提出三项面向工程落地的检测改进：
>
> **(1)** 在骨干 P3/P4/P5 三层级层次化注入 CBAM 注意力增强；
>
> **(2)** 新增 P2（stride=4）小目标检测分支，专门优化 finger 类（占训练框 37%、中位尺寸 28.9 px）的小尺寸目标召回；
>
> **(3)** 显式利用 11353 张无缺陷负样本，从机制上抑制误检。
>
> 在公平的 200 epoch / 余弦 LR 训练协议下，本文在 PVEL-AD val 集报告系统消融结果：CBAM 层次化注入贡献 **[A1-A0] pp**，P2 分支贡献 **[A3-A0] pp**，二者协同贡献 **[A4-A1-A3+A0] pp**。CBAM 注入位置消融证实层次化 vs 单层级的相对优势。在 RTX 5060 Ti 上，本文方法 FPS = **[FPS]**，参数量 = **[params]M**，满足无人机机载实时检测要求。本文同时给出预训练权重迁移率随结构改动的量化分析与工程应对策略，为后续在 ultralytics yaml 上做结构改造的研究者提供经验参考。

### 5.2 第 1 章绪论

- 1.1 不动
- 1.2 不动
- 1.3.1 研究内容稍微改：明确"系统消融分析"为研究主体而非"性能提升"
- 1.3.2 三项创新点说法保留（CBAM 层次化 / P2 分支 / 负样本利用），但每项加一句"本文通过 EX 节实验**严谨验证**该改进的真实贡献"
- 1.3.2 末尾的 "0.5874" 数字**删除**，改成"本文给出在公平协议下三项改进的量化贡献分解"

### 5.3 第 3 章方法

- 3.1.3 数据集挑战分析**全部重写**：基于实测数据
  - "极端长尾"改为"训练集 1158× 长尾"（不是 7000×）
  - "90% < 0.05"改为"32% < 0.05 但 finger 类（37% 框）中位 28.9 px 处于 P3 边界"
  - "小目标主导"改为"finger 类的小尺寸主导"
- 3.4.1 P2 分支动机**全部重写**：基于 finger 类 28.9 px 中位的事实，不再说 "4-8 像素"
- 3.4.3 P2 计算开销待 E6 跑完后填

### 5.4 第 4 章实验

按 E1-E9 重组：
- 4.1 实验设置（v2 协议）
- 4.2 Baseline 横评（E1）
- 4.3 主消融（E2，特别报告 finger 类 AP）
- 4.4 CBAM 注入位置消融（E3，新增章节）
- 4.5 训练预算与预训练迁移分析（E4 + E5，新增章节）
- 4.6 复杂度（E6）
- 4.7 鲁棒性（E7）
- 4.8 可视化（E9）
- 4.9 部署（E8）
- 4.10 综合讨论 — **诚实写**：哪些改进有效、哪些无效、为什么

### 5.5 第 5 章结论

- 5.1 工作总结：撤回"性能提升 X pp"叙事，强调"系统消融 + 工程经验"
- 5.2 主要创新点：**只保留实验真正支撑的**——如果 E3 证明 CBAM 层次化无效，创新点 1 就改成"CBAM 在 P3 单层注入足够"
- 5.3 局限性：保留+加上"未在更大 GPU 平台做更长 epoch 训练"
- 5.4 展望：保留+加上"基于 E5 from-scratch 经验的训练预算建议"
- 5.5 写在最后：保留风格

---

## 6. 不动的事情

- 第 2 章相关理论：完全不动
- 数据集划分：不动（11353 负样本融入 train/val 的策略保留）
- `models/register_modules.py`：不动（patch 工作正常）
- `code/configs/*.yaml`：除 E3 新增的 3 个外不动
- 现有 `runs/baseline/` `runs/ours/` `runs/ablation/`：**保留**作为"短预算/线性 LR 对照"，论文里可以提（也可以不提）

---

## 7. 风险与应对

### 风险 1：E2 跑完发现 ours < baseline

**应对**：
- 报告事实
- 5.3 局限性写入：在 nano 规模 + 200 ep 预算下，结构改进未能补偿迁移率损失
- 5.4 展望写：更大模型规模 / 更长 epoch / 更大 GPU 上的验证
- 论文仍然成立——核心贡献是 **"系统消融分析"**，不是"超过 baseline"

### 风险 2：E3 跑完发现 P3+P4+P5 ≈ P3 only

**应对**：
- 撤回创新点 1 的"层次化"措辞
- 改成"P3 单层 CBAM 已足，P4/P5 边际收益小"——这本身是一个工程经验
- 1.3.2 创新点对应改

### 风险 3：跑了 250 小时还有失败的步骤

**应对**：所有训练脚本设计成**断点续训**（ultralytics 原生支持 resume），失败后能从 last.pt 接着跑，不浪费已有 epoch。

---

## 8. 待审决策清单（你拍板）

1. **新定位（"系统消融 + 工程经验"代替"性能 SOTA"）** —— 同意/不同意
2. **优先级**（必做 / 推荐做 / 完整）—— 选哪个
3. **训练协议**（200 ep / cos_lr / lrf=0.01 / close_mosaic=20 / patience=30）—— 同意/微调
4. **公平性约束**（所有配置都用 yolo11n.pt 做预训练源、from-scratch 仅在 E5 单独评估）—— 同意/不同意
5. **数据集事实修正**（撤回 7000× 长尾、90% 小目标、4-8 px 这三个错误说法）—— 同意/不同意
6. **风险叙事**（如果 ours < baseline 就如实写）—— 接受/讨论备选

如果有任何一项要讨论、需要补充、或者有更好方案，告诉我。审过后我才开始写代码。
