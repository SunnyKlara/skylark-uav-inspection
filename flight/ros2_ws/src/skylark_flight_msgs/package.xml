<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>skylark_flight_msgs</name>
  <version>0.1.0</version>
  <description>
    Skylark 巡检平台飞控层的接口契约：action / msg / srv 定义。
    本包是 flight/ 与 edge/ 之间唯一的通信约定，不含任何实现代码，也不依赖任何具体飞控。
    换飞控（PX4 / ArduPilot / 其他）时本包不变，只换 skylark_autopilot_iface 的实现。
  </description>

  <maintainer email="noreply@example.com">SunnyKlara</maintainer>
  <license>AGPL-3.0-or-later</license>

  <url type="repository">https://github.com/SunnyKlara/skylark-uav-inspection</url>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>rosidl_default_generators</buildtool_depend>

  <depend>action_msgs</depend>
  <depend>builtin_interfaces</depend>
  <depend>geometry_msgs</depend>
  <depend>std_msgs</depend>

  <exec_depend>rosidl_default_runtime</exec_depend>

  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>

  <member_of_group>rosidl_interface_packages</member_of_group>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
