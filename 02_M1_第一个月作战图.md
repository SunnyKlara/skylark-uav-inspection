# M1 第一个月作战图：把工具链跑通

> 目标：4 周后你能独立训练一个光伏缺陷检测模型，并产出第一张 baseline 对比表。
> 投入：每周 20 小时，每周 5 天，每天 4 小时。
> 心态：不要追求理解每行代码，先让它跑起来。**先动手再看书**。

---

## Week 1：环境搭建（2 天就能搞定，留时间补理论）

### Day 1-2：装环境

#### 路线 A（推荐）：Windows + WSL2

你电脑装的是 Windows 没错吧？最稳的方案是 **WSL2 + Ubuntu 22.04**。

**为什么不直接用 Windows 训练**：
- 99% 的 SOTA 仓库都是 Linux 优先
- PX4 仿真后期需要用（如果你想做加分项）
- 装一次 WSL2 一劳永逸

**操作步骤**：

```powershell
# 在 PowerShell（管理员）里执行
wsl --install -d Ubuntu-22.04
# 重启电脑
# 启动 Ubuntu，设用户名密码
```

进入 Ubuntu 后：

```bash
# 1. 验证 GPU 通到 WSL（关键！）
nvidia-smi
# 必须看到你的 5060 Ti 才算成功

# 2. 装 miniconda
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh

# 3. 创建环境
conda create -n yolo python=3.11 -y
conda activate yolo

# 4. 装 PyTorch（注意 CUDA 12.1 版本）
pip install torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu121

# 5. 装 ultralytics（YOLO 官方包）
pip install ultralytics

# 6. 验证
python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"
# 应该输出 True 和 NVIDIA GeForce RTX 5060 Ti
```

#### 路线 B（备选）：纯 Windows

如果 WSL 装不好，纯 Windows 也行：

```powershell
# 1. 装 Anaconda
# 官网下载：https://www.anaconda.com/download

# 2. 打开 Anaconda Prompt
conda create -n yolo python=3.11 -y
conda activate yolo

# 3. 装 PyTorch
pip install torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu121

# 4. 装 ultralytics
pip install ultralytics

# 5. 验证
python -c "import torch; print(torch.cuda.is_available())"
```

> ⚠️ **5060 Ti 是最新一代显卡，必须 CUDA 12.x 以上 + PyTorch 2.4 以上**，老版本不识别。

### Day 3：跑通 YOLOv11 推理

```bash
# 在你装好的环境里
yolo predict model=yolo11n.pt source='https://ultralytics.com/images/bus.jpg'
```

第一次运行会自动下载权重。结果保存在 `runs/detect/predict/`。能看到带框的图片就成了。

### Day 4-5：训一个 demo

下载 COCO128（YOLO 官方教学数据集，128 张图）：

```bash
yolo train model=yolo11n.pt data=coco128.yaml epochs=10 imgsz=640
```

5060 Ti 上 10 个 epoch 大概 5 分钟。训完看 `runs/detect/train/results.png`，能看到 loss 曲线就行。

**Week 1 验收标准**：你能独立 (1) 推理一张图 (2) 训练一个 demo。

---

## Week 2：数据集 + Baseline

### Day 1：下载光伏公开数据集

我帮你列了 5 个候选，按推荐度排序：

| 名称 | 类型 | 链接关键词 | 推荐 |
|---|---|---|---|
| **PVEL-AD** (Photovoltaic Electroluminescence Anomaly Detection) | EL 图像，10 类缺陷 | 搜 "PVEL-AD" GitHub | ⭐⭐⭐⭐⭐ |
| **Solar Panel Soiling Image Dataset** | RGB，污染检测 | Kaggle 搜 "solar panel soiling" | ⭐⭐⭐ |
| **Photovoltaic Module Defect** (Roboflow) | RGB，多缺陷 | Roboflow 搜 "photovoltaic" | ⭐⭐⭐⭐ |
| **InfraredSolarModules** | 红外，11 类异常 | GitHub "RaptorMaps/InfraredSolarModules" | ⭐⭐⭐⭐⭐ |
| **PV-Multi-Defect** | RGB+IR，论文配套 | 跟随论文获取 | ⭐⭐⭐ |

**推荐组合**：PVEL-AD（主数据集）+ InfraredSolarModules（鲁棒性验证）+ Roboflow 那个（自定义补充）。

> 我下一份文档会给你**每个数据集的具体下载步骤**。这周先把 PVEL-AD 拿到手就行。

### Day 2-3：数据集格式转换

YOLO 需要这种格式：

```
dataset/
├── images/
│   ├── train/
│   ├── val/
│   └── test/
├── labels/
│   ├── train/
│   ├── val/
│   └── test/
└── data.yaml
```

写个简单脚本把公开数据集转成 YOLO 格式。这一步是体力活，但**不要让 AI 全代写**——这是你最容易拿来在论文 4.1 节炫耀的"工程能力"。

### Day 4-5：跑 4 个 baseline

```bash
# Baseline 1: YOLOv8n
yolo train model=yolov8n.pt data=your_data.yaml epochs=100 imgsz=640 device=0

# Baseline 2: YOLOv10n
yolo train model=yolov10n.pt data=your_data.yaml epochs=100 imgsz=640 device=0

# Baseline 3: YOLOv11n（你的主基线）
yolo train model=yolo11n.pt data=your_data.yaml epochs=100 imgsz=640 device=0

# Baseline 4: RT-DETR
yolo train model=rtdetr-l.pt data=your_data.yaml epochs=100 imgsz=640 device=0
```

5060 Ti 单个 baseline 训练 2-4 小时，你可以挂着睡觉跑。

**Week 2 验收标准**：你有一张 baseline 对比表（4 行 × 5 列：模型 / mAP@0.5 / mAP@0.5:0.95 / Params / FPS）。

---

## Week 3：开始读论文 + 想清楚改进点

### 第一周读 5 篇必读论文（按顺序）

**精读（每篇花 2-4 小时）**：

1. **YOLOv11 论文**（ultralytics 技术报告）
   - 你必须把主基线吃透
2. **YOLOv8 论文**（同上）
3. **CBAM**（CVPR 2018）
   - 注意力机制鼻祖，写进你的 related work
4. **BiFPN / EfficientDet**（CVPR 2020）
   - 多尺度融合鼻祖
5. **任意一篇 2023-2024 年的"YOLO + 光伏"中文核心论文**
   - 模仿它的写作 + 找到它的弱点

**略读（10 分钟扫一遍）的另外 10 篇**：搜 "UAV photovoltaic defect detection deep learning"，按引用量排前 10 全部扫一遍。

### 改进点候选清单（从这里挑 2-3 个）

| 改进点 | 工作量 | 预期涨点 | 推荐 |
|---|---|---|---|
| 加 CBAM 注意力到 backbone | ⭐ | +1-2% mAP | ✅ 必做 |
| 加 EMA 注意力（更新版） | ⭐ | +1-3% mAP | ✅ 必做 |
| neck 换成 BiFPN | ⭐⭐ | +1-3% mAP | ✅ 必做 |
| 增加 P2 小目标检测头 | ⭐⭐ | +2-4% mAP（小目标多时） | ✅ 推荐 |
| 改用 SIoU / WIoU 损失 | ⭐ | +0.5-1% mAP | ⭐ 加分 |
| 加 GhostConv 轻量化 | ⭐⭐ | -1% mAP / -30% Params | ⭐ 加分 |
| 知识蒸馏 | ⭐⭐⭐ | 复杂 | ❌ 不建议 |

> 你的最终方案 = **CBAM/EMA + BiFPN + P2 检测头**，三件套打包成你的"创新方法"。

---

## Week 4：第一次"动刀"改 YOLOv11

### Day 1-2：clone 一份 ultralytics 源码本地修改

```bash
git clone https://github.com/ultralytics/ultralytics.git
cd ultralytics
pip install -e .
```

源码版可以直接改网络结构。

### Day 3-5：实现第一个改进（最小可行版）

先就改一个 —— **加 CBAM 到 backbone**。

具体改哪几个文件、加哪几行代码，我们到这周再细说，那时你已经熟了。

**Week 4 验收标准**：你跑出一组"YOLOv11 + CBAM"的训练结果，跟纯 YOLOv11 对比能看到 mAP 变化（涨跌都正常，重点是能跑）。

---

## 一个月小结：你应该交付的东西

```
✅ 工具链：PyTorch + ultralytics + GPU 全部跑通
✅ 数据集：PVEL-AD（或同类）下载完成 + YOLO 格式转换完成
✅ 基线：4 个 baseline 跑完，第一张对比表
✅ 文献：5 篇精读 + 10 篇略读
✅ 改进：CBAM 第一版改完，跑出第一组实验
```

**输出物清单**：

```
E:\Users\Administrator\Desktop\gp\graduation_project\
  └── M1_outputs\
       ├── env_setup_screenshot.png       # 环境验证截图
       ├── dataset_stats.md               # 数据集统计报告
       ├── baseline_table_v1.md           # 4 baseline 对比表
       ├── literature_notes\              # 5 篇精读笔记
       │    ├── YOLOv11_notes.md
       │    ├── YOLOv8_notes.md
       │    ├── CBAM_notes.md
       │    ├── BiFPN_notes.md
       │    └── benchmark_paper_notes.md
       └── first_improvement_log.md       # CBAM 第一版日志
```

---

## 你每天的"傻瓜流程"

```
早上：开 IDE → 看进度表 → 今天做什么
中午：跑实验（让它后台训）
下午：读论文 / 改代码
晚上：写日志（哪怕 50 字也行）+ 同步给我
```

---

## 卡住了怎么办

**永远只问三个问题**：

1. 我现在的目标是什么？（看这份文档对应的 Week / Day）
2. 我卡在哪一步？（具体到错误信息或现象）
3. 我已经尝试了什么？

带着这三条来找我，我能 5 分钟给你答案。来一句"装不上 PyTorch 怎么办"，我会陪你磨半小时。

---

## 下一步

把这份 M1 通读一遍，然后告诉我：

- **Day 1-2 想用哪条路线**（A: WSL2 / B: 纯 Windows）？
- **是否需要我立刻给你 Day 1 的"逐字逐句操作手册"**？

你说"来"，我就把 Day 1 拆成"复制即可执行"的 50 行指令。
